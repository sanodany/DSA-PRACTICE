function DuplicateChecking(array){
    let seen = {}
    for(let i=0;i<array.length;i++){
        if(seen[array[i]]){
            return array[i]
        }else{
            seen[array[i]] = true
        }
    }
    return -1
}
console.log(DuplicateChecking([1, 3, 5, 2]))
